# Doc Scripts (Maintenance)

These scripts are for document writing/refactor batches.

They are not required for normal CAPL/DBC development or verification runs.

For daily operations, use:

- `python scripts/run.py ...`

Use files in this folder only when you are explicitly doing document refactor work.
